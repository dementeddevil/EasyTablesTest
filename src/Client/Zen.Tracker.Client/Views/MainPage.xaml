<?xml version="1.0" encoding="utf-8" ?>
<views:AsyncContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                        xmlns:views="clr-namespace:Zen.Tracker.Client.Views;assembly=Zen.Tracker.Client"
                        x:Class="Zen.Tracker.Client.Views.MainPage">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Button x:Name="logoutButton"
            Text="Logout"
            Clicked="LogoutButton_OnClicked"
            Grid.Row="0"
            Grid.Column="1" />

    <Label Text="Welcome to Xamarin Forms!"
           VerticalOptions="Center"
           HorizontalOptions="Center"
           Grid.Row="1"
           Grid.ColumnSpan="2" />
  </Grid>

</views:AsyncContentPage>